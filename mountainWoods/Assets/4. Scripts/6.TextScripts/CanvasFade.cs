using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CanvasFade : MonoBehaviour {

   /* public IEnumerator FadePanelIn(CanvasGroup canvas)
    {
        float percentage = 0;
        while (percentage < 1)
        {
            percentage += 0.05f;
            canvas.alpha = percentage;
            yield return new WaitForSeconds(0.1f);
        }
    }

    public IEnumerator FadePanelOut(CanvasGroup canvas)
    {
        float percentage = 1;
        while (percentage > 0)
        {
            percentage -= 0.05f;
            canvas.alpha = percentage;
            GetComponent<CanvasRenderer>().SetAlpha(percentage);
            yield return new WaitForSeconds(0.1f);
        }
    }*/
}
